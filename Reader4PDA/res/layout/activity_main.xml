<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                        xmlns:tools="http://schemas.android.com/tools"
                                        android:id="@+id/dr_Layout"
                                        android:layout_width="fill_parent"
                                        android:layout_height="fill_parent"
                                        android:background="@color/background_second">

    <android.support.v4.widget.SwipeRefreshLayout
            android:id="@+id/sw_Layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                        android:layout_width="fill_parent"
                        android:layout_height="fill_parent"
                        android:orientation="vertical">

            <ListView android:id="@+id/lv_4PDA"
                      android:layout_width="match_parent"
                      android:layout_height="match_parent"
                      android:listSelector="@android:color/transparent"
                      android:divider="@android:color/transparent"
                      android:padding="3dp"
                      android:dividerHeight="3dp"/>

            <ImageView android:id="@+id/iv_DinoInternet"
                       android:layout_width="wrap_content"
                       android:layout_height="wrap_content"
                       android:visibility="gone"
                       android:src="@drawable/img_dino_internet"
                       android:layout_centerInParent="true"/>

        </RelativeLayout>

    </android.support.v4.widget.SwipeRefreshLayout>

    <ListView android:id="@+id/lv_Options"
              android:layout_width="240dp"
              android:layout_height="match_parent"
              android:layout_gravity="start"
              android:dividerHeight="1dp"
              android:choiceMode="singleChoice"
              android:divider="@color/divider_options"
              android:listSelector="@drawable/list_options"
              android:background="@color/background_second"/>

</android.support.v4.widget.DrawerLayout>
